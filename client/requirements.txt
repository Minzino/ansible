requests>=2.31.0
inquirer>=3.1.0
rich>=13.7.0
python-dotenv>=1.0.0 # To manage API endpoint URL
colorama>=0.4.6 # For colored terminal output in log viewer
tabulate>=0.9.0 # For displaying data in tables (if needed, list_clusters uses rich Table now) 